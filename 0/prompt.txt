Ran `git push` from my local client and https://github.com/conallob/talos-rpi-builder/tree/claude/talos-v1.13.7-release now exists.

Since this repo is a fork, I think we want to skip the PR process and just merge our changes directly to main, because it is part of a fork network. We may also want to add that decision tree wrt PRs to my account-wide CLAUDE.md